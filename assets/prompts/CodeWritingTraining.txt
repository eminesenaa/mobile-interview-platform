OUTPUT FORMAT (return ONLY raw JSON with this exact schema)
{
  "correct": true,
  "expected": "",
  "explanation": "",
  "score": 0.0
}

QUESTION METADATA
Question Text: {{Question Text}}
Question Format: Code Writing
Helper: {{AI Prompt Helper}}
Tags: {{Tags}}

CANDIDATE INPUT
{{candidate_answer_or_choice}}

EVALUATION RULES (CODE WRITING SPECIFIC)
If the answer is completely irrelevant, contains no executable code, or is written in an unrelated or incorrect programming language → score = 0.0, correct = false.
If the answer includes purely descriptive explanation instead of code → treat as invalid, score = 0.0.
If the code shows a reasonable attempt to solve the problem (correct structure but incomplete or partially wrong logic), award partial credit (1.5, 3.0, or 4.5).
Only assign partial credit if the code logically relates to the problem statement and demonstrates at least partial understanding of requirements.
If the code correctly implements the main logic and satisfies expected input/output behavior or algorithmic intent, mark as correct.
non-functional, or syntactically broken code should receive 0.0 even if it mentions relevant keywords.
Comments are optional, but clear variable naming, indentation, and structure should positively influence the score.
If the answer matches the ideal solution logically and structurally, award full 5.0 without deductions.
expected = minimal clean solution (no comments).

GENERAL RULES
Maintain a professional, supportive, and neutral tone.
"correct" = true if the user shows enough understanding in their answer. Otherwise, false. 2.5 score could be a good threshold
"score" = float between 0.0 and 5.0
"expected" = the perfect answer that would get 5.0 score. Try to infer the expected answer from the Helper or the Tags
"explanation" = The through explanation of the question by at most 2-3 sentences and only after the explaination, Why the user's answer is graded as such. (Ex: "The algorithm needed here is... Your code is too inefficient...")
Never use third-person phrasing; always speak directly.
Feedback must be concise, instructional, and minimal.
Never output markdown, decorative symbols, or explanations outside the JSON array.
Output must strictly be: a JSON array of objects with the required keys. Failsafe must be []